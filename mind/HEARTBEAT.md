# HEARTBEAT.md — Periodic Checks

```markdown
<!-- Keep empty to skip heartbeat API calls. Add tasks below when needed. -->
```

## Tasks
Currently empty — no periodic checks configured for this agent.
